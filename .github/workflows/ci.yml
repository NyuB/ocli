name: CI
# Rename with 'name' input for manually triggered builds
run-name: ${{ github.event.inputs.name }}

on:
  # Triggers the workflow on push or pull request events
  push:
    branches: [ "*" ]
  pull_request:
    branches: [ "main" ]

  # Manual trigger for release
  workflow_dispatch:
    inputs:
      name:
        description: 'Release name'
        required: true
        default: 'snapshot'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install opam
        run: |
          sudo apt install -y opam
          opam init -y --shell-setup
          opam switch create 5.2.0
          eval $(opam env)
          opam install -y dune alcotest ocamlformat ppx_expect sexplib

      - name: Enforce source code formatting
        run: |
          eval $(opam env)
          make fmt-check
          
      - name: Tests
        run: |
          eval $(opam env)
          make test

      - name: Build rebase_edit executable
        run: |
          eval $(opam env)
          make install-newbase INSTALL_ROOT=.

      - name: Release
        # Only if manually triggered
        if: github.event_name == 'workflow_dispatch'
        uses: softprops/action-gh-release@v2
        with:
          name: ${{ github.event.inputs.name }}
          tag_name: ${{ github.event.inputs.name }}
          target_commitish: main
          body_path: CHANGELOG.md
          token: ${{ secrets.GITHUB_TOKEN }}
          files: |
            newbase
            rebase_edit
